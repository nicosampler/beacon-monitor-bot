version: "3.8"

services:
  db:
    image: postgres:17
    container_name: beacon-monitor-only-postgres
    env_file:
      - .env.local
    ports:
      - "5432:5432"
    volumes:
      - beacon_postgres_data:/var/lib/postgresql/data

volumes:
  beacon_postgres_data:
